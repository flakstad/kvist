(package main)

(defn render-request [path: string] -> string
  (str "@get('" path "', {openWhenHidden: true})"))

(defn main []
  (let [request (render-request "/updates") :defer]
    (println request)))
