(package order-independent)

(def default-box-value: int 17)

(defn read-box [] -> int
  (let [box (make-box 42)]
    box.value))

(defn read-default-box [] -> int
  (let [box (Box {})]
    box.value))
