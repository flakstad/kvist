;; Copyright (c) Andreas Flakstad and Kvist contributors
;; SPDX-License-Identifier: MIT

(package order-independent)

(def default-box-value 17)

(defn read-box [] -> int
  (let [box (make-box 42)]
    box.value))

(defn read-default-box [] -> int
  (let [box (Box {})]
    box.value))
